#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../contributors/bin/contributors" "$@"
  ret=$?
else 
  node  "$basedir/../contributors/bin/contributors" "$@"
  ret=$?
fi
exit $ret
